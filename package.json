{
  "name": "ewg-email-template",
  "private": true,
  "scripts": {
    "build": "mjml index.mjml -o index.html && mjml index-emailify.mjml -o index-emailify.html",
    "validate": "mjml --validate index.mjml; mjml --validate index-emailify.mjml",
    "serve": "node serve.js"
  },
  "devDependencies": {
    "mjml": "^4.18.0"
  }
}
